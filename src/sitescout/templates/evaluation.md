# SiteScout evaluation: {label}

Everything below is a **{label}**, generated by `scripts/evaluate.py` from the pipeline's own outputs. It checks whether the ranking is plausible against the few public chargers that exist; it is not a measure of how right the ranking is, and the ranking is not ground truth.

## A. Backtest

Backtest mode removes every existing charger from every feature before scoring (SPEC §5). A **hit** is a candidate within {hit_radius_m} m of a known charging site.

- Known charging sites: **{known_sites}** (OSM charging stations merged within {merge_radius_m} m; manual charger list: {manual_status}).
- Hit candidates: **{hit_candidates}** of {candidates}. SiteScout's top {top_k} holds {hits_in_top} of them.

Precision and Recall are measured over the **candidate ranking**: a hit is a candidate, not a charging site. Recall@{recall_at} is the share of all hit candidates (candidates within {hit_radius_m} m of a known charging site) that rank in the top {recall_at}. The number of hit candidates and the number of distinct known charging sites can differ: several candidates can lie near the same site, and a site can have no candidate near it.

| Metric | SiteScout [{level}% interval] | Population-only (`{population_feature}`) [{level}% interval] | Random (mean of {random_seeds} seeds) | SiteScout − population [{level}% interval] |
|---|---|---|---|---|
{metric_rows}

SiteScout {verdict}. {certainty}

**Read with care.** With {hit_candidates} hit candidates, one hit more or less moves {top_metric} by {one_hit}; the intervals are wide and no strong claim follows. In backtest mode the charging-gap component is the same for every candidate (SPEC §5), so this tests the other four components. The OSM data postdates the chargers, so mapping around them may still carry a trace of them.

## B. Weight stability

Each of the {weights} feature and profile weights was multiplied by {low_factor} and by {high_factor}, its group renormalised to 1, and the Top-{stability_k} (production mode) compared with the unperturbed Top-{stability_k}.

- Mean overlap **{mean_overlap}**, minimum **{min_overlap}**; target {target}.
- Perturbations below the target: **{below_target}** of {runs}.

The ten least stable perturbations:

| Weight | Factor | Top-{stability_k} overlap |
|---|---|---|
{stability_rows}

## C. Network effect

Added in Milestone 6 (exact MCLP, greedy and Top-30 compared).

## D. Data quality

- Candidates: {dq_candidates}; invalid coordinates: {invalid_coordinates}; duplicate ids: {duplicate_ids}; duplicate coordinates: {duplicate_coordinates}.
- Missing feature values (production): {missing}.
- Manual charger list: {manual_status}.

Grid-layer completeness by district (a proxy for how much of the grid is mapped; national median {national_median} mapped power features per km²):

| District | Mapped power features | Per km² | Ratio to median |
|---|---|---|---|
{grid_rows}

## E. Grounding

Added in Milestone 7 (site briefs).
